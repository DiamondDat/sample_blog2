<%= form_tag(user_generate_new_password_email_path(@user)) do %>
  <%= submit_tag 'Send request Password Email', class: 'btn btn-primary',
                  :confirm => 'Are you sure you want to email reset password instructions?' %>
<% end %>
